function sumaNums(a: number, b: number): number {
    return a + b
} 

//que pasa si yo quisiera concatenar strings con esta funcion?
function sumaConcatString(a: string, b: string): number | string {
  return a + b
};